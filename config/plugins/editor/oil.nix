{
  plugins.oil = {
    enable = true;
    settings = {
      columns = [
        "icon"
      ];
      keymaps = {
        "-" = {
          action = "Oil";
          options = {
            desc = "Open parent directory";
          };
        };
      };
    };
  };
}
